node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Impeccable runtime state (pid/port/session token, per-session hook cache), not project source
.impeccable/live/
.impeccable/hook.cache.json
